#include <bits/stdc++.h>
using namespace std;

void solve() {
   int a,d;
   cin>>a>>d;
   if(a<=d)cout<<"Yes";
   else cout<<"No"; 
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
        solve();

    
    return 0;
}